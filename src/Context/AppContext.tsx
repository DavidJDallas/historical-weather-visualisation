import * as React from 'react';
import {createContext, useState} from 'react';

//useContext hook is a technique for passing data down the component tree without having to pass props down manually at every level. Used by creating a provider component and often by creating a hook to consume the value in a child component. 

